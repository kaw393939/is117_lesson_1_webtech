# Node + npm (Minimal)

Node.js lets JavaScript run on your computer. npm is the package manager.

Check versions
```bash
node -v
npm -v
```

Initialize a project
```bash
npm init -y
```

Result
- A `package.json` file appears in your project root.